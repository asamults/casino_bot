{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "PROMETHEUS" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 10,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(casino_bot_game_rounds_total{game_id=~\"$game_id\",status=\"committed\"}[$__rate_interval])) by (outcome)",
          "legendFormat": "{{outcome}}"
        }
      ],
      "title": "Committed rounds / s (by outcome)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "PROMETHEUS" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 11,
      "targets": [
        {
          "expr": "sum(rate(casino_bot_game_rounds_total{game_id=~\"$game_id\",status=\"committed\",outcome=\"win\"}[$__rate_interval])) / sum(rate(casino_bot_game_rounds_total{game_id=~\"$game_id\",status=\"committed\"}[$__rate_interval]))",
          "legendFormat": "win_rate"
        }
      ],
      "title": "Committed win rate (ratio)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "PROMETHEUS" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 12,
      "targets": [
        {
          "expr": "sum(rate(casino_bot_game_round_rejected_total{game_id=~\"$game_id\"}[$__rate_interval])) by (code)",
          "legendFormat": "{{code}}"
        }
      ],
      "title": "Engine reject rate by code",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "PROMETHEUS" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 13,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(casino_bot_game_round_duration_seconds_bucket{game_id=~\"$game_id\"}[$__rate_interval])) by (le, game_id))",
          "legendFormat": "{{game_id}} p95"
        }
      ],
      "title": "Game round latency p95 (s)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "PROMETHEUS" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 },
      "id": 14,
      "targets": [
        {
          "expr": "sum(rate(casino_bot_game_token_volume_total{game_id=~\"$game_id\"}[$__rate_interval])) by (direction)",
          "legendFormat": "{{direction}}"
        }
      ],
      "title": "Token volume rate (stake / payout)",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["casino_bot", "games", "phase4b"],
  "templating": {
    "list": [
      {
        "allValue": ".*",
        "current": { "selected": true, "text": "All", "value": "$__all" },
        "datasource": { "type": "prometheus", "uid": "PROMETHEUS" },
        "hide": 0,
        "includeAll": true,
        "label": "game_id",
        "multi": true,
        "name": "game_id",
        "options": [],
        "query": "label_values(casino_bot_game_rounds_total, game_id)",
        "refresh": 2,
        "regex": "",
        "sort": 1,
        "type": "query"
      }
    ]
  },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Casino Bot - Games (Phase 4B)",
  "uid": "casino-bot-games",
  "version": 1
}
